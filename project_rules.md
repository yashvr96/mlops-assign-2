# User Preferences & Project Rules

1. **Python Environment**: Always use `venv` for virtual environments.
2. **Python Version**: Use Python 3.12.
3. **Terminal Commands**: Use `;` as the command separator (Windows PowerShell style) instead of `&&`.
4. **Server**: Use `uvicorn` or similar depending on requirements.
